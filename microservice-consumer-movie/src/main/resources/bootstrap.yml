spring:
  application:
    name: microservice-consumer-movie
  cloud:
    config:
      profile: dev
      label: master
      discovery:
        enabled: true
        serviceId: config
#      username: user
#      password: admin
#  rabbitmq:
#    host: localhost
#    port: 5672
#    username: guest
#    password: guest
eureka:
  client:
    healthcheck:
      enabled: true
    service-url:
      defaultZone: http://user:admin@eureka:8761/eureka/
management:
  security:
    enabled: false